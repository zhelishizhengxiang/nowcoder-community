<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simon.community.dao.mybatis.CommentMapper">

  <sql id="Base_Column_List">
    id, user_id, entity_type, entity_id, target_id, content, `status`, create_time
  </sql>
    <select id="selectCommentsByEntity" resultType="com.simon.community.pojo.Comment">
    select <include refid="Base_Column_List"></include>
        from comment
        where entity_type=#{entityType} and entity_id=#{entityId} and status=0
        order by create_time desc
        limit #{offset},#{limit};
  </select>

  <select id="selectCountByEntity" resultType="java.lang.Integer">
    select count(comment.id) from comment
        where entity_type=#{entityType} and entity_id=#{entityId} and status=0
  </select>
    <select id="selectCommentById" resultType="com.simon.community.pojo.Comment">
        select <include refid="Base_Column_List"></include>
        from comment
        where id=#{id}
    </select>

    <insert id="insertComment">
      insert into comment
      values(null,#{userId},#{entityType},#{entityId},#{targetId},#{content},#{status},#{createTime});
  </insert>

</mapper>